# tp-wed : en el tp wed 2/ruter.php - en la linea 8 hay un error que evita mostrar el home al iniciar " if (isset($_GET['action'])) {" el "isset"esta mal puesto hay deveria ir "!empty" esta sentencia
